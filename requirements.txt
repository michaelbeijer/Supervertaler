# Supervertaler Dependencies
# Install all required packages: pip install -r requirements.txt

# Core UI Framework
PyQt6>=6.5.0
PyQt6-WebEngine>=6.5.0

# Document Processing
python-docx>=0.8.11
openpyxl>=3.1.0
Pillow>=10.0.0
lxml>=4.9.0

# AI/LLM Providers
openai>=1.0.0
anthropic>=0.7.0
google-generativeai>=0.3.0
requests>=2.28.0

# Document Conversion & Analysis
markitdown>=0.0.1

# Translation Quality Metrics (Superbench)
sacrebleu>=2.3.1

# Clipboard Operations (Universal Lookup)
pyperclip>=1.8.2

# Keyboard Control (AutoFingers)
keyboard>=0.13.5

# PDF Processing (PDF Rescue)
PyMuPDF>=1.23.0

# Audio Processing (Voice Dictation)
numpy>=1.24.0
sounddevice>=0.4.6

# Semantic Search (Supermemory)
sentence-transformers>=2.2.0
chromadb==0.6.3
tokenizers==0.22.0

# YAML Support
pyyaml>=6.0.0

# Markdown Rendering (Superdocs)
markdown>=3.4.0

# Spellchecking
pyspellchecker>=0.7.0
cyhunspell>=2.0.0

# Machine Translation APIs
boto3>=1.28.0  # Amazon Translate
deepl>=1.15.0  # DeepL API

# ===== Optional Dependencies =====
# Uncomment the sections below to enable optional features

# Voice Dictation (Supervoice)
# Requires: ffmpeg installed on your system
# openai-whisper>=20230314
# sounddevice>=0.4.6

# Automation Features (AutoFingers/Universal Lookup)
# Note: AutoFingers is Windows/memoQ-specific
# pyautogui>=0.9.53
ahk>=1.0.0  # Python wrapper for AutoHotkey (Windows only, for Superlookup global hotkey)

# ===== Platform-Specific Notes =====
# - Linux: tkinter is not needed (removed from imports)
# - Windows: All features available including AutoFingers
# - macOS: All features available except AutoFingers
# - Supervoice: Requires ffmpeg system package on all platforms
